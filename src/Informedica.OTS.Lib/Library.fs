namespace Informedica.OTS.Lib

// STUB — placeholder for future implementation (Ontology Terminology Server /
// Google Sheets-CSV data access). Currently empty: the live Google Sheets
// access actually lives in Informedica.Utils.Lib.Web.GoogleSheets.
// Removed from GenPRES.sln to speed up build/test; re-add the project (and a
// ProjectReference from its consumers) when real implementation begins.

module Say =
    let hello name = printfn "Hello %s" name
